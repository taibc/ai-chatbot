# AI PDF Chatbot (Local LLM)

Chatbot web sử dụng LLM chạy local (Ollama) để đọc và trả lời câu hỏi từ file PDF.

## Cách chạy
```bash
pip install -r requirements.txt
ollama run llama3
streamlit run web.py
